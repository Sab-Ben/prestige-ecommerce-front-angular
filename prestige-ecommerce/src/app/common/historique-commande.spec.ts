import { HistoriqueCommande } from './historique-commande';

describe('HistoriqueCommande', () => {
  it('should create an instance', () => {
    expect(new HistoriqueCommande()).toBeTruthy();
  });
});
